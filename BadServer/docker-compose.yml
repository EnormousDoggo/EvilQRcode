version: '3.8'

services:
  web:
    build: .
    container_name: badserver
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./emails:/var/www/html/emails
    networks:
      - nginx-proxy-network

networks:
  nginx-proxy-network:
    external: true